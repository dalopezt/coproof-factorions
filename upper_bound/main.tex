% Label: Upper Bound

**Theorem (Upper Bound).** For every natural number $n$ with $n \geq 10^7$, the digit-factorial sum of $n$ is strictly less than $n$. That is, if $S(n)$ denotes the sum of the factorials of the decimal digits of $n$, then
$$n \geq 10^7 \implies S(n) < n.$$

*Proof.* Unsolved.