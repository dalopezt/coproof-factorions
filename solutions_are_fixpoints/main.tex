% Label: Solutions Are Fixpoints

**Theorem (Solutions Are Fixpoints).** For each $n \in \{1, 2, 145, 40585\}$, the sum of the factorials of the decimal digits of $n$ equals $n$ itself. That is,
$$\sigma(n) = n,$$
where $\sigma(n) = \sum_{d \,\in\, \text{digits}(n)} d!$

*Proof.* Unsolved.